<?xml version="1.0" encoding="UTF-8"?>
<launch>
    <param name="fixed_robot_description"
        command="$(find xacro)/xacro.py '$(find davinci_description)/robots/davinci_fixed_links.xacro'" />

    <!-- Show in Rviz   -->
    <node name="joint_state_publisher" pkg="joint_state_publisher" type="joint_state_publisher">
        <param name="use_gui" value="true"/>
	<param name="robot_description" value="/fixed_robot_description" />
    </node>

</launch>
